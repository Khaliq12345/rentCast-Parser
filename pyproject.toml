[project]
name = "rentcast"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hrequests>=0.9.2",
    "pymongo[srv]>=4.15.2",
    "python-dotenv>=1.1.1",
]
